environment:
  name: "*"
  description: Input file for 'cfc init/update  --environment custom.env.yaml
  settings:
    defaultAdmin: cfadmin
    initialPassword: D3m0!234
  settingLists:
    blobStagedFolders:
      - ${artifactsStagedBlobs}
    fileStagedFolders:
      - ${artifactsStagedFiles}
  settingObjects:
